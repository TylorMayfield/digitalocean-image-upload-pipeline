packages:
  - name: image
    functions:
      - name: process
        runtime: nodejs:22
        web: true
        webSecure: ${FUNCTION_AUTH_TOKEN}
        limits:
          timeout: 60000
          memory: 1024
        environment:
          SPACES_BUCKET: ${SPACES_BUCKET}
          SPACES_REGION: ${SPACES_REGION}
          SPACES_KEY: ${SPACES_KEY}
          SPACES_SECRET: ${SPACES_SECRET}
          SPACES_CDN_BASE_URL: ${SPACES_CDN_BASE_URL}
